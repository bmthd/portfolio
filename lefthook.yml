pre-commit:
  parallel: true
  jobs:
    - name: check lint
      run: npm run check:lint
    
    - name: check format
      run: npm run check:format
    
    - name: check similarity
      run: npm run check:similarity
