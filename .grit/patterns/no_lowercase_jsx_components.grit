language js

or {
  `<$tag />`,
  `<$tag $_ />`,
  `<$tag>$_</$_>`,
  `<$tag $_>$_</$_>`
} where {
  $tag <: r"^[a-z][a-z0-9-]*$",
  register_diagnostic(
    span = $tag,
    message = "Do not use direct DOM elements. Use the project's design system instead.",
    severity = "error"
  )
}
